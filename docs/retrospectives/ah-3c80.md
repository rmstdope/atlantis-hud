# ah-3c80 — retrospective

- **Implementer:** Storm
- **Date:** 2026-08-19
- **PR:** #454

## The apt stall this bead bounds hit three of three CI runs on the bead's own PR

**What happened.** The PR that adds `timeout-minutes: 15` to the browser jobs stalled in
`Install the Playwright browser` on its first run (`pwa` and `smoke (desktop-shell, 1, 2)`, both cut
at 15m) and again on the first re-run (`smoke (desktop-shell, 1, 2)`, cut at 15m). Only the second
re-run was green. Logs show apt hanging mid-download — `fonts-ipafont-gothic` at 14:11:39, the next
line at 14:17:06, then cancellation — the same shape as the four sightings the bead cites.
**Why.** GitHub Actions' Ubuntu apt mirror, which nothing in this repository controls. The ceiling
worked exactly as designed: 15 minutes instead of the 25–55 previously observed, and re-runnable.
**Cost.** About 55 minutes of wall-clock across two re-runs — and zero runner-hours held past 15
minutes, which is what the bead set out to buy.
**Prevent by.** `ah-f9q9` (the pinned Playwright container) is the fix that removes the exposure
rather than bounding it, and this run is evidence its priority is higher than "filed alongside":
three consecutive stalls in one hour means the failure is now common rather than occasional. Whoever
triages `ah-f9q9` should read this section as its fifth-through-seventh sightings.
**Seen before.** ah-k6i.5, ah-bn6.1, ah-mjy, ah-vw63 — all the same step.
