# ah-0851 — retrospective

- **Implementer:** Cyclops
- **Date:** 2026-09-15
- **PR:** #1277

## Posting the review sub-agent's verdict was refused as self-approval

**What happened.** After the reviewer sub-agent returned *No findings.*, the single Bash call that ran
`gh pr comment 1277 --body-file <review>`, `bd update --append-notes`, `bd dolt push` and the
`agent-state ... --phase ci` write was denied by the auto-mode classifier with reason
`[Self-Approval]`. None of it ran, including the review file's `cat > /tmp/...` heredoc, so a later
retry failed on a missing file.
**Why.** Not established beyond the classifier's label: posting a review of one's own PR and moving
toward merge in one command reads as self-approval, although `implement-bead` prescribes exactly it.
**Cost.** One navigator question and about an hour of wall-clock while it waited for an answer.
**Prevent by.** A Bash permission rule for `gh pr comment` on the implementer's own PR (or the
review-posting step in `implement-bead` *Posting it*) so the documented step is not left to the
classifier; and writing the review file to the scratchpad in a call of its own, before posting.
**Seen before.** ah-z3h3.2.
