# UI design proposals (issue #17)

Design artefacts for issue #17, "Basic UI for the app". These are **mockups**, not application
code — they exist so the layout could be chosen before implementation starts.

For the graphical design of the map itself as implemented, see [hex-rendering.md](hex-rendering.md).
Five candidate directions for a future hex design are drawn in
[hex-design-proposals.html](hex-design-proposals.html), and
[map-themes.md](map-themes.md) describes the theme engine those designs plug into, including how to
add or remove a map theme. All five designs drawn in the proposals ship, and the map opens on
Cartographer's Table. A sixth theme, Classic — the map as it looked before the designs arrived —
was retired once they had all landed.

## Files

| File | What it is |
|---|---|
| `layout-proposals.html` | The four candidate layouts, side by side, with trade-offs for each. |
| `chosen-layout-turn71.html` | The chosen layout (variant 04) drawn from a real turn 71 report. |
| `mockups/*.png` | Rendered screenshots of both pages, referenced from issue #17. |
| `orders-export-descriptions.html` | Where the "keep the unit descriptions" option lives on the orders export (bead ah-52b). Three candidates; A, a second item in the Export menu, was chosen. |
| `region-problems-toggle.html` | What the control that hides the region panel's Problems section looks like (bead ah-f8u). Three candidates; 1, a chip in the panel header, was chosen. |
| `faction-view.html` | Where the faction's attitudes, allowances and unclaimed silver are read (bead ah-vp3.2). Three placements and three ways of setting out 32 declared factions; A, a popover anchored off the faction name in the header, was chosen, with a line per attitude level. |
| `region-decorations.html` | How a province is drawn on the map (bead ah-6zv). Three treatments, three rules for a province found in two pieces, and three homes for the switch; C, the atlas label, was chosen, every connected piece is outlined and named, and the switch is a `Regions` entry in the Badges menu. |
| `battles-view.html` | Where the turn's battles are read (bead ah-1is.2). Three candidates; B, one dialog with the battle list beside the battle, was chosen, and a list line states its outcome neutrally as attacker and defender losses. |
| `region-borders.html` | How bold a province outline needs to be to read against the hex lattice at every zoom (bead ah-f9c). Treatments over three simulated theme grounds and a far-zoom strip; Option B1 — a 3px screen-constant dashed line over a 5.5px screen-constant halo, in each theme's own ink — was chosen. |
| `panel-split-resize.html` | How the drag handle between the unit panel and the orders editor announces itself (bead ah-13o). Two live, draggable candidates plus the folded and limit-reached states; B, the always-visible grip pill in the existing gap, was chosen. |
| `hex-notes.html` | Where a player's own note on a hex is written and how it is drawn on the map (bead ah-o1t, issue #261). Two homes for the editor, three map treatments and two switch placements, plus the states; A — a Notes section in the region panel — was chosen, with a pin on the map that opens into a stack of tags on click, and a `Notes` entry in the Badges menu. |
| `units-pane-drag-resize.html` | Dragging the "Units in hex" pane to size it, replacing the row-count setting (bead ah-2r3). Two live candidates for what a dragged height means on a short list, snap vs free pixels, the bounds, and the folded, keyboard, empty and short-window states; A — the dragged height is the height — with free pixels, a one-row floor and a 70% ceiling was chosen. |
| `order-ocd-late-vocabulary.html` | What Order OCD does when the core's word list arrives after the editor is already open — the bug where the first unit opened after a page load is never tidied (bead ah-1znc). Two options across four cases; A — the late tidy always runs, whatever has been typed meanwhile — was chosen, with the word the caret is inside left alone, no undo step, no notice, and turning the setting on mid-session tidying the open block immediately. |
| `order-ocd-indent.html` | Order OCD gaining two more rules (bead ah-2tj8): one space of indentation per level inside `FORM`/`TURN`, and exactly one newline at the end of a unit's orders. Three sections — C (indent both as you type and on the whole block when the tidy runs), i (`END` and `ENDTURN` align with their openers), i (trailing blank lines collapse when the tidy runs, not continuously, so Enter is never absorbed). |
| `order-ocd-indent-edges.html` | The cases ah-2tj8's happy path hides. A (an unclosed `FORM` or a stray closer indents by the running depth anyway, so the runaway indent is the signal), i (comment lines indent like orders, blank lines stay truly empty), i (a unit with no orders stays empty rather than gaining a blank line), W1 (the settings description names all three behaviours plainly). |
| `order-ocd-indent-keys.html` | Keys, undo and paste for ah-2tj8. B (Backspace stays stock CodeMirror and deletes one space), i (Enter and its indent are one undo step; the whole-block tidy is not undoable at all, as the case tidy already is not), P1 (pasted orders are uppercased and re-indented as they land, in the same transaction, so one Ctrl+Z removes the whole paste). |
| `backup-import-collision.html` | What importing a game backup does when that game is already here (bead ah-c0m). Three candidates plus the states; B — an inline Replace / Keep both / Cancel confirmation in the picker's *This game* tab — was chosen, with the copy named “<name> (imported)”. |
| `rename-game.html` | Where a game is renamed (bead ah-lkw). Three homes plus the states; A — a Rename link on the Name row of the picker's *This game* tab, opening an in-place field with Save / Cancel — was chosen; an empty name is refused with creation's words, a duplicate only warns. |
| `map-levels.html` | What the player sees once the nexus and the underworld are levels of their own rather than filed on the surface (bead ah-4b4). Four questions with the states drawn: the level control uses the report’s own words (nexus / surface / underworld, shallowest first), the nexus is remembered like any hex, an unexplored hex off the surface is described “in the underworld”, and hex labels stay `terrain (x,y) in Province`. |
| `status-line-tones.html` | Which header status-line messages a player sees and in what colour once the line has a tone of its own rather than import flags (bead ah-9gk). Three notice treatments, a tone table for every message the shell writes, and three rules for the turn-messages chip on a failure; A — a visible notice with a dim grey dot — was chosen, the tone table as proposed, and a failure never hides the chip. |
| `form-alias-reused.html` | What the player sees when the same FORM number is used twice in a hex this month (bead ah-yk7). Two anchorings and the states; A — one warning per repeat, on the repeat, naming the first use — was chosen, per hex as the rules scope it, with a new *Orders* group in the Warnings tab. |
| `fleet-cannot-sail.html` | What the player sees when a fleet ordered to SAIL is overloaded or short of sailors (bead ah-j0e). Three scopes, two message shapes and the states; scope B (this month's boarding counted, GIVEs later) with two codes — *Overloaded fleets* and *Undercrewed fleets* in a new *Sailing* group — was chosen, wording as drawn. |
| `fleet-load-after-loading.html` | What the overload warning's number means once this month's loading is priced into it (bead ah-eet, the follow-up to ah-j0e). Settles four things across three rounds: the number is shown as a sum so it agrees with the report's `Load:` line; everything the ledger prices counts — GIVE, TAKE, BUY and SELL, all of which the server runs before movement, correcting the bead's own premise; the verb is *loaded* / *unloaded*; and an order nothing can price falls back to the report's weights rather than silencing the fleet. A unit boarding counts as weight loaded, so the first number is always the report's own figure. |
| `give-target-not-here.html` | What the player sees when a GIVE or TAKE names a unit that is not in the hex to receive it (bead ah-djq). Reading the five real engine refusals in the corpus reshaped the bead: only two are mistyped numbers, the other three name a live unit standing in another hex. Chosen: the test is *is it in this hex*, and the line says which of the two mistakes it is; the wording follows `taught-not-here`'s *is not in this hex to be taught*, giving four sentences across GIVE/TAKE and known/unknown; the toggle *Gifts to units that are not here* joins the existing *Orders* group, on by default. A stealthed friendly unit is an accepted false positive; silver gifts are not exempted, since three of the four confirmed errors are silver. |
| `unit-overloaded.html` | What the player sees when a MOVE order would overload the unit issuing it (bead ah-yfo). The report already prints both numbers and the route planner already refuses an overloaded unit; the gap is the hand-written MOVE. Chosen: the check weighs the unit as it will be when it moves, pricing this month's GIVE/TAKE/BUY/SELL in as ah-eet does for fleets — because dropping the ballast and walking off in the same month is the standard fix for this mistake, and the corpus shows a player doing exactly that. The message names both numbers and ends *so it will not move*, matching the fleet warning and staying true on an ADVANCE line. Toggle *Overloaded units* joins the existing *Orders* group. Capacity itself is not repriced. |
| `buy-not-for-sale.html` | What the player sees when a BUY names an item the region's market does not offer (bead ah-d8u). The three real refusals in the corpus name units that appear in no committed report, so the design is argued from the mistake the report's layout invites instead: *For Sale* is the shop and *Products* is what the peasants can be set to make, and only the first can be bought. Chosen: the message names that confusion when the item is on the Products line, and a hex with nothing for sale says so rather than printing an empty list — four sentences composing those two facts. The market list is never truncated; the largest For Sale line in the corpus has four items. Toggle *Buying what is not for sale* joins the existing *Orders* group. |
| `too-many-trade-regions.html` | What the player sees when PRODUCE orders span more regions than the faction's allowance permits (bead ah-8om). The arithmetic was verified by hand: `Trade Regions: 15 (15)` against orders in 16 regions, and the sixteenth holds exactly the two units the report errors on. This is the first warning that belongs to the *faction* rather than to a hex, and a hexless finding is silently dropped by the client — so the page is really about where such a warning can live. Chosen: it lands on the first PRODUCE order in the document, one line for one mistake. A faction-wide row of its own was judged the honest answer and deliberately deferred as a second bead. The message gives the count, the limit and the consequence, in three forms covering both report schemas and a faction that may not trade at all. |
| `interface-size.html` | What the player sees when the panes can be scaled from inside the app (bead ah-46p.2, the second half of ah-46p). Chosen: a slider from 100% to 200% in steps of 25%, in the Global tab beneath *Pane transparency* and in the same shape; row heights in the units dock follow the text so nothing clips, while pane widths stay as the player dragged them; default 100%, so nothing changes until asked. The map never scales — every piece of its text carries its own explicit size, which is what ah-46p.1's rem tokens were kept clear of. |
| `order-argument-completion.html` | What the orders editor's completion popup offers after the order name, not only for the first word of a line (epic ah-bai). Nine decisions across two rounds: closed keyword lists, the six directions with IN/OUT and the item classes now, the ruleset's items and skills as a second bead; quiet until a letter is typed with Ctrl+Space to summon; silence when nothing matches; Enter or Tab accepts, unchanged; and a trailing space is left behind *everywhere*, the command position included. Entries are bare words in the ruleset's own order, the 22 item classes alphabetically as the single exception. |
| `trade-routes.html` | Finding a hex selling a good another will pay more for (epic ah-1j5). Three rounds against real fixture data — which showed that a single turn's report almost never contains a route at all, while one game's remembered turns yield seven. The answer is a hex pair carrying every good tradeable between the two, ranked by quantity × margin over the known map with each half dated, distances given as `14/7/4 turns on foot/riding/flying`, and both directions folded into one circuit where each way pays. It lives behind a counted *Trade* chip in the header, as Problems does; a row click flies the map to the buying hex. Capacity is not modelled. |
| `study-at-maximum.html` | What the player sees when a unit is ordered to study a skill it has already taken to the ruleset's maximum (bead ah-1uj). Three wordings and three homes for its toggle; “this unit is already at observation 5, the highest the ruleset has” was chosen — naming the skill by the ruleset's name and saying whose maximum it is — with the toggle joining the Warnings tab's Teaching group, renamed *Studying/Teaching*, as *Study with nothing to learn*. |
| `about-tab.html` | Settings → About gaining prose that says what the app is (bead ah-o2li). Three copy variants drawn in the dialog; Cerebro's revision was chosen, with the variant names read from `RULESETS` rather than written into the copy, spelled `New Origins` everywhere, and the prose placed below the Version/Build rows and above the update control. Also records the accuracy fix: “a web based client” contradicted the Build row directly above it on every desktop build. |
| `about-tab-variant.html` | How the About tab names the variants a build ships, once they come from `RULESETS` (bead ah-o2li). Three shapes shown with one variant and with two; S3 was chosen — a `Variants` row beside Version and Build, with the prose keeping no sentence about it at all, so it grows to any number without touching a word of copy. |
| `problem-walk.html` | The F8 problem walk gaining a visible position, and keeping its place across a re-validation (bead ah-9ess). Filed as a flaky smoke test; reading the code found the walk's position is a `useRef` nothing renders from, and that every re-validation silently resets it to the start. B was chosen — a `3/7` counter between the `‹ ›` arrows — with R1, the walk keeping its place by identity rather than jumping back to the top. |
| `problem-walk-edges.html` | The awkward cases for ah-9ess. K1 (fixing the problem you stand on resumes at the first one at or after it, so nothing is skipped or repeated), N1 (no position means no counter — before the first step, with no problems, and after fixing the one you were on), W2 (on a narrow window the counter drops out and the arrows stay). |
| `launchers.html` | How an agent is started, and whether the seven `run-*` shims survive (bead ah-qled.5.3). Three options with the commands drawn as a terminal, plus a table of what each shim starts today and which are named for the role rather than the agent. C was chosen — drop them all for `launch <Name>`, with the roster as the single place a fleet is declared — after the mockup discussion turned up two facts: the Emacs fleet view already calls `launch` for every agent, and `CLAUDE.md` documents a `run-bishop` that has not existed since the Bishop→Forge rename. |
| `phase-vocabulary.html` | Whether `--phase` stays a closed list of eighteen words (bead ah-qled.5.2). A consumer that adds a role of its own invents a phase word for it, and `agent-state` exits 2 on every call — so that role cannot report state at all. A was chosen — any lower-case word is accepted — with the guard kept but rewritten: `cerebro--phases` becomes a reference list, and the test now asserts it has not drifted from the words the shipped agents actually use, rather than asserting legality. |
| `release-in-any-project.html` | Cutting a release in a project that is not this one (bead ah-qled.7.3). Cerebro's release procedure hardcodes `pnpm run release`, a gate described as "lint, typecheck, unit tests, rustfmt, clippy, rust tests", and a GitHub Actions workflow literally named `Release` that builds a macOS bundle. A was chosen for the post-tag watch — a declared `release_watch` key, absent meaning there is nothing to watch — and i for the gate, describing what it is for rather than what it contains. |
| `harness-scripts.html` | Two harness scripts that are cerebro's job but live in this project's `scripts/` and are reached through its package manager (bead ah-qled.7.2). `retroSightings.ts` is ported to bash + awk in cerebro — chosen after counting what cerebro already depends on: `awk` appears in 8 of its scripts and `node` in none — with the tests ported first, because the parser's `None found` counter-example trap is the kind of thing a rewrite loses silently. For the disk preflight, i was chosen: cerebro owns the check and the project declares `disk_floor_gb`, killing the coupling where `prune-worktrees.sh` greps a consumer's TypeScript for a number. |
| `consumer-ci.html` | How the consumer-fixture CI job behaves while its sibling beads are still landing (bead ah-qled.11). The bead's own description recommends building it red; that cannot be done, because nothing merges red and the job would block every PR in the repository until the last sibling landed. B was chosen — green by construction, asserting only what passes today, with a commented-out stub naming the bead that owes each remaining assertion. |
| `ah-bxgs-arrival.html` | What the *receiving* unit's ITEMS row does when goods are transported to it from another hex (bead ah-bxgs). A was chosen: the row moves, in the same italic every other projection uses, with the hover naming the sender and stating that transport resolves last so the goods cannot be spent this month. Leaving the receiver alone was rejected as the worse half of the defect — goods would leave one row and appear on none — and a distinct arrival glyph was rejected for putting a second mark in a cell that already carries `+ ?`. |
| `ah-bxgs-unshown-target.html` | What happens to the *sender* when the target is a unit the report does not show — an ally's quartermaster, which is the ordinary case, or a typo (bead ah-bxgs). U1 was chosen: the goods leave and the hover says where to, mirroring what the column already does for a `TAKE` from a source it cannot see. Marking the cell `+ ?` was rejected for showing the legitimate case as doubt; behaving like a misdirected `GIVE` and doing nothing at all was rejected because a player who wrote an order would see a row that looks as if they wrote nothing. |
| `ah-bxgs-refused.html` | What the column does with goods the game will not transport — men, mounts, ships and war machines, which the shipped catalogue can identify for certain (bead ah-bxgs). R1 was chosen: the refused goods stay put and the hover says why, so the column stays right about what the unit will hold. Moving them anyway was rejected as wrong on two rows at once; marking the cell `+ ?` was rejected because the figure is certain and the mark would say the opposite of the truth. |
| `ah-bxgs-doubtful.html` | The items the catalogue cannot classify, and whether they may be transported (bead ah-bxgs). The navigator's own rule was adopted in place of the three options offered: **anything a `create …` spell can make is an artefact and cannot be transported.** It measures cleanly — 21 such skills, 18 naming an item that exists, all 18 requiring artifact lore, and the three that name no item make auras and phantasms, which are already refused as summoned creatures. It takes the refusal set to 105 of 171 items and leaves five that no rule reaches, which stay transportable rather than being listed by hand. |
| `ah-bxgs-strings.html` | Every sentence the transport work adds, as it ships (bead ah-bxgs). W1 — each departure gets its own line, so the player can read off every transport they wrote and see which did nothing, keeping the two ends of the order symmetric. T1 — the timing clause is stated per arrival, in the shape the column already uses for production, rather than once at the end in a second wording of a shipped sentence. |
| `ah-bxgs-states.html` | The states the happy path hides (bead ah-bxgs): the three forms of the order, more ordered away than the unit holds, a sale that runs first and leaves nothing to send, and targets that are not units. N1 was chosen for a row where as much arrives as leaves — upright, hover still speaking — so italic keeps its exact meaning of *this figure differs from your report*. Making italic mean *the orders did something here* was rejected for redefining it across the whole table inside a bead about one order. |
| `ah-bxgs-hover-order.html` | Two details of the hover (bead ah-bxgs). H1 — arrivals join the other `Includes …` lines and departures go together after everything the unit gained, ahead of the *cannot be counted* line, so gains read as one block and losses as another; gathering all five transport lines at the end was rejected for pulling arrivals away from the family they belong to. P2 — the refusal sentence names the item by **tag**, because the plural in a report follows what the unit holds rather than what is being sent, so `1 horse` would otherwise read "will not transport horse". |
| `consumer-traps.html` | Where a project's own hard-won traps live, and the CLAUDE.md cerebro requires but does not ship (bead ah-qled.10.5). A was chosen for curation — Forge proposes a traps entry from a retrospective's `Prevent by` during its sweep and the navigator accepts or declines, since all 138 retrospectives have one and the filter is the hard part — and ii for the template, a working default meant to be edited rather than a form to fill in. Also records a correction to the bead: most of the existing traps are already generic and universal, so the work is a split rather than the deletion the bead asked for. |
| `no-men-warning.html` | Exempting a unit with no men from the "has no order that spends the month" warning (bead ah-udff). This revises ah-dwk6's explicit "there are no exemptions", so the mockup records it as an amendment rather than a fix. A was chosen — the exemption keys on the men count alone, whatever else the unit holds — and i, silently skipped rather than replaced by an observation line. Also records the finding that `men == 0` alone is unsafe: `men_estimated` is true when the ruleset could not read a unit's items, and `Unit::default()` is `men: 0, men_estimated: true`. |
| `ah-agbm-buy-amount.html` | What quantity a pending `BUY` puts in the ITEMS column when the market line is oversubscribed (bead ah-agbm). Three options drawn against a worked case — 12 grain for sale, 20 and 10 asked by two of our own units. A was chosen: the **settled** figure, the same settlement the Silver column already charges against, so the two cells on one row cannot contradict each other. Showing the asked figure was rejected because ITEMS and SILVER would then disagree in plain sight; refusing to project was rejected because an oversubscribed line is the ordinary case. |
| `ah-agbm-partial-doubt.html` | What happens to a unit's other orders when one of them cannot be counted at all — `BUY ALL`, a whole class, goods the market does not sell (bead ah-agbm). C was chosen: count everything countable and let the cell admit the gap with a trailing mark. Counting the rest silently was rejected (a confident figure that omits an order), and letting one bad order blank the whole cell was rejected (a knowable sale goes unshown and the row looks like a unit with no orders). |
| `ah-agbm-withdraw.html` | Whether the ITEMS column shows goods from a `WITHDRAW` when the faction's unclaimed fund may not cover everything asked of it (bead ah-agbm). A was chosen: the goods are shown, and the overdraft keeps its existing single faction-wide warning — warning twice about one mistake is what ah-bumi was filed to stop, and the Silver column already charges a withdrawing unit nothing. Withholding was rejected because a faction-wide fact would blank a hex-local column in unrelated hexes. |
| `ah-agbm-list-shape.html` | What the ITEMS line does with goods that appear from nothing and stocks that run out (bead ah-agbm). A was chosen: keep exactly the rule `GIVE` already uses — an emptied stock vanishes, a new one is appended at the end — because one rule for every order that moves items is what the bead exists for. Keeping an emptied stock as `0 FUR` and sorting new goods in by tag were both rejected for changing shipped `GIVE` behaviour the bead did not ask about. |
| `ah-agbm-the-mark.html` | Where the "part of this month could not be counted" mark sits, drawn at the column's real 220px width with a list that genuinely overflows it (bead ah-agbm). M1 — the mark trails the list — was chosen, with its cost accepted and recorded: on a long list the ellipsis eats the mark, and the hover is then the only thing carrying it. Also settles that a unit whose *only* order cannot be counted stays upright but carries the mark, so italic keeps its exact meaning of "this is a projection". |
| `ah-agbm-glyph-and-strings.html` | The mark's glyph and every string as it ships (bead ah-agbm). G2, `+ ?`, was chosen — it reads as arithmetic, *this list plus something unknown*, and cannot be mistaken for another entry in the list; a bare `?` was rejected as reading like a stray item and `… ?` for colliding with the cell's own truncation ellipsis. The hover names the order that could not be counted rather than the rule, and on a unit with both kinds of note the counted goods are stated before the gap. |
| `ah-jown-the-cell.html` | What the ITEMS column shows for a `BUY ALL` once it is resolved into a concrete quantity (bead ah-jown), drawn against the shipped contradiction it removes — SILVER already says the unit spent its money while ITEMS still shows the money and a `+ ?`. A was chosen: the settled figure, plain, reading exactly as a stated `BUY` does. Keeping the `+ ?` was rejected for giving one glyph two meanings — "could not be counted" and "counted from an estimate"; a `≤` ceiling mark was rejected because every settled `BUY` in the table is equally a ceiling, so the glyph would either spread to all of them or be arbitrary here. |
| `ah-jown-hover-and-edges.html` | What the ITEMS hover says about a `BUY ALL`, and the three states the happy path hides (bead ah-jown): a unit that can afford none, two `BUY ALL` lines on one unit, and two of our own units splitting one market line. H2 was chosen — the hover names the cap that bit — because it is the only option with anything to say in the states where the player has written an order that will do nothing; saying nothing extra and a single fixed sentence were both rejected on that ground. Z2 and M2 follow from it: the "buys none" case explains itself in the hover, and a line split between our own units gets a sentence naming that cause rather than blaming the market. |
| `ah-jown-round2-words.html` | The wording as it ships, drawn beside a shipped `PRODUCE` sentence (bead ah-jown). W2 was chosen: the sentences take the shape `capSentence` already gives `PRODUCE` and `CAST`, which the same hover already prints — the round-1 wording was rejected once seen next to it, as two voices in one tooltip. S1 was chosen: the SILVER hover prints the sentence too, the arrangement `productionCapSentence` was exported for, so the cell that visibly dropped from 356 to 14 explains itself. Also settles that a `BUY ALL` of men needs no wording of its own. |
| `ah-jown-round3-zero.html` | The two ways a `BUY ALL` buys nothing (bead ah-jown) — cannot afford one, or our own units claimed the line — which have opposite remedies. Z-B was chosen: a dedicated sentence for each, leading with the order being dead. The general sentence was rejected because it degrades at zero into "has silver for no grain, not the 10 this market offers"; one cause-free sentence was rejected because the player cannot then tell whether to send silver or to rewrite a faction-mate's order. Also settles the exact tie, where neither cap really bit: the silver sentence speaks. |
| `ah-3sp7.2-refusal-reach.html` | How far the refusal of un-giveable items reaches, once the catalogue can resolve an item class (bead ah-3sp7.2). Three questions the family's own interview never reached, each drawn as the rows it produces: whether `ALL ITEMS` warns as well as changing its rows, what the sentence's tail says when several kinds of item survive, and what happens to `GIVE 901 20 LION`, which names an un-giveable item directly. 1A, 2A and 3B were chosen — one rule for every form of the order, every survivor named with its count, and the named-item form getting both the refusing row and the sentence. Warning only for the narrow classes was rejected for leaving two rules where there could be one; a fixed-length tail was rejected for losing the counts; leaving the named form alone was rejected because the application would then say two different things about the same lions depending on how the order was typed. This is what amends the check's Settings description from "of a whole class" to "naming items". |
| `ah-3sp7.2-round2-edges.html` | The two edges neither the rules nor the data page settles (bead ah-3sp7.2). `GIVE 0` discards rather than gives, and the restriction is worded "cannot be given to **other units**" — 1B was chosen, reading that sentence literally: the lions go and nothing is said. Treating a discard as a transfer was rejected as a claim the pages do not support; leaving it uncountable was rejected for keeping a permanent `?` on the one shape where the application now has every fact but one. And the count printed when an order asks for more than the unit holds — 2A was chosen, what would actually have moved, which is the rule the class form already uses and can never print a number the unit does not have. |
| `ah-gjbs-shape-branches.html` | The magic study tree grouped by branch of study (bead ah-gjbs), drawn from the 70 magic skills in the shipped `ruleset.json`. **Chosen**, as the default view. A skill's branch is computed — follow its first prerequisite up to the one-step skill — never a hand-written table, so a ruleset refresh places a new skill without anyone editing code. Ten cards; the eight one-step skills nothing further needs are collected into *Straight from a foundation*. A prerequisite from another branch shows as a violet `+INVI 3` chip rather than a line drawn across the page. |
| `ah-gjbs-shape-layered.html` | The same 70 skills as one layered graph, foundations left, every prerequisite an arrow (bead ah-gjbs). **Chosen as the second view**, behind a *Whole graph* toggle. The honest picture and the expensive one: it is a DAG, not a tree — 34 of the 70 skills have two or more prerequisites — and the tiers are 4 / 15 / 38 / 11 / 2, so the middle tier alone is about 1100px tall and the view always pans and zooms. |
| `ah-gjbs-shape-lens.html` | One skill at a time: a filterable list, and the chosen skill's fully expanded ancestry above and what it unlocks below (bead ah-gjbs). Live — click a skill or type in the filter. **Not chosen**: it answers *what do I need before this* in one reading and is much the cheapest to build, but it never shows the shape of the whole tree, which is what the bead asks for. Its per-skill reading may return as a follow-up. |
| `ah-gjbs-states.html` | What the happy path hides for ah-gjbs, plus two complete wording tables. Settles: landing on a skill from a mage's unit pane scrolls to that branch and picks the row out; a narrow window drops to one column and never splits a card; a ruleset that failed to load says *The game data has not loaded.* rather than drawing an empty tree; there is no loading state, the ruleset already being in memory before the dialog can open. The **plain** wording set was chosen — *Magic study tree*, *Branches* / *Whole graph*, *Apprenticeship*, *Show in study tree* — over the game's own register. The view opens on **F3**, next to the dictionary's F2, and the Branches/Whole graph choice is remembered for the session but not across a reload. |
| `ah-gjbs-interaction.html` | The four remaining decisions for ah-gjbs, as side-by-side pairs. Chosen: **no filter** (the view shows structure, and finding a named skill is the dictionary's job); a skill name **opens that skill in the dictionary** while a violet cross-branch chip **jumps to that skill's own row in the tree**, so the graph's cross edges can be walked; the cap rule is stated **once, under the header** — *A magic skill can never rise above the skills it stands on — the levels below are floors to begin, and ceilings thereafter* (`rules/magic_skills`); and the unit pane offers **one link per mage**, at the top, landing the tree on that unit's highest magic skill. |
| `ah-1t41-a-clear.html` | What the units pane's filter box does when the list under it is replaced (bead ah-1t41). Option **A, chosen**: changing the list empties the box, silently, with no toast and no undo. Draws the defect first — filter *All my units* down to one unit, click the Army you were building, and the Army reads as empty while the rail beside it says twelve. |
| `ah-1t41-b-remember.html` | Option **B, rejected** for the same bead: each source keeps a filter of its own, so the text returns when you come back to the list you typed it on. Rejected because the box would then change under the player in both directions — a filter typed and forgotten reproduces the original symptom on purpose — and because it disagrees with `ah-1mpx.4`, which clears the multi-selection outright when the source changes. |
| `ah-1t41-r2-states.html` | Round 2 of ah-1t41: the three cases the wider rule reopened, each drawn both ways. Settles that a **new turn's report keeps** the filter (nothing was pointed at, and comparing one turn against the next is what the box is for); a **hex click while an Army is the source keeps** it (the list did not change); the Army on screen being **deleted clears** it; and that the box is **not refocused** after clearing, so the map keeps its keyboard. |
| `ah-1mpx.6-r1-where.html` | Where a foreign unit's battle-derived combat skills show, and how much there is to show (epic ah-1mpx.6). Three cumulative scopes; C — the export, the units table's Skills column and the Unit panel — was chosen, over the export alone, which leaves the table saying *not disclosed* about skills the application is holding. Counts the yield over the committed reports: 143 of 252 foreign units covered in a battle-heavy turn, and nothing at all in 14 of the 26. |
| `ah-1mpx.6-r1-mark.html` | How a battle-derived skill is told apart from one the report actually showed (epic ah-1mpx.6). Three treatments; M2 — the turn in the cell, `COMB 5 (turn 71)`, sitting where a known skill puts its study points — was chosen over colour alone and over a `from battle:` lead-in. Records the evidence that levels move: 70 of 595 repeated observations disagreed, 68 of them upward, and no unit ever lost a skill it had shown. |
| `ah-1mpx.6-r2-states.html` | The cases ah-1mpx.6's happy path hides. T2 (the turn collapses when every derived skill came from one turn, falling back to per-skill when they differ), D1 (the newest observation wins), E1 (*not disclosed* holds until the asynchronous scan lands, then the cell fills in), U1 (the Unit panel section is headed *Skills from battle reports*, so the heading carries the caveat), F1 (the scan reads every stored turn of the game, whoever's report it is). |
| `ah-1mpx.6-r3-words.html` | The wording ah-1mpx.6 ships with. W1 (one export notice split by the count, replacing a sentence about to become false) and H1 (the fullest hover: the skills as words, the battle's hex, the turn, and the rule stated once). Draws the row that carries two turns at once — a remembered Army member last *seen* at turn 71 whose crossbow a battle at turn 72 disclosed — and confirms three rules an implementer would otherwise have to guess. |

The layout pages answered issue #17 and are kept as the record of that decision. Pages added later
answer one bead each and are named after the question they settle, not after the layout — the table
above says which bead each belongs to.

Open any HTML file directly in a browser. They are self-contained: no build step, no external
assets, no network access. The hex maps are inline SVG generated by a short inline script.

## The four candidates

| # | Name | Map area | Unit table width |
|---|---|---|---|
| 01 | Classic Commander | Large, never overlaid | Full window |
| 02 | Three-Column Workbench | ~Half the window | Centre column only |
| 03 | Map-First Inspector | Whole window | Narrow drawer, 3–4 columns |
| **04** | **Docked Inspector** *(chosen)* | **Whole window** | **Full window, collapsible** |

Variant 04 was chosen because it resolves the trade-off that made 01 and 03 each imperfect: the
unit table needs horizontal room for faction, men, silver, skills, items and structure, while the
map wants the whole window. Making the table a collapsible overlay rather than a fixed row gets
both, and moving report state into the application header removes the one panel that never needed
to sit next to the map.

### 01 — Classic Commander

Map left, region above unit in a right rail, units table beside orders in a bottom dock. The shape
every reference client converged on. Nothing ever overlays the map, but a third of the window is
permanently chrome.

![Variant 01, Classic Commander](mockups/variant-01-classic-commander.png)

### 02 — Three-Column Workbench

Reports and region pinned left, map and units table centre, unit and orders right. Every widget
permanently visible and the simplest to build and test, but the map is squeezed to about half the
window.

![Variant 02, Three-Column Workbench](mockups/variant-02-three-column-workbench.png)

### 03 — Map-First Inspector

Full-bleed map with floating drawers. Best map area, but the unit table is confined to a narrow
drawer that fits only three or four columns.

![Variant 03, Map-First Inspector](mockups/variant-03-map-first-inspector.png)

### 04 — Docked Inspector (chosen)

Full-bleed map, report state in the header, a screen-wide collapsible units dock, region left, unit
and orders right.

![Variant 04, Docked Inspector](mockups/variant-04-docked-inspector.png)

Collapse all four panels and the map opens to the full window, each panel leaving a title strip:

![Variant 04 with all panels collapsed](mockups/variant-04-collapsed.png)

## The chosen layout on real data

Drawn from a real NewOrigins turn 71 report: Borg TNG (95), December Year 6, 4,028 lines,
11 regions in this turn's report, 45 more known by name, 92 units in the selected hex.

![Variant 04 on turn 71, own unit selected](mockups/turn71-01-expanded.png)

Selecting a **foreign** unit still fills the unit panel — foreign units are legitimate to inspect —
but the orders panel refuses the edit rather than offering a disabled text box:

![Foreign unit selected, orders read only](mockups/turn71-02-foreign-unit-locked.png)

Selecting a **previously visited** region shows the data held from the last sighting, banner-stamped
so it can never be mistaken for current. Orders are refused for a second, distinct reason: the unit
is not in the current turn.

![Previously visited region selected, data stamped as of turn 71](mockups/turn71-03-stale-region.png)

Collapsed, showing all four map states at once — current, previously visited (hatched and
age-faded), known by name (faded), and unexplored:

![All panels collapsed, whole world visible](mockups/turn71-04-collapsed.png)

## What these mockups established

Drawing against real NewOrigins reports rather than placeholder data surfaced several things that
changed the implementation plan:

- **Hexes are flat-top.** Atlantis exits are N, S, NE, SE, NW, SW; a direct northern neighbour means
  flat-top, not pointy-top. The mapping is `px = x * 1.5R`, `py = y * (sqrt(3)R/2)`, and only
  coordinates where `x + y` is even are valid. `packages/shared/src/PixiHexMap.tsx` currently draws
  pointy-top, and `packages/shared/src/mapData.ts` parses `"A1"`-style region ids that this ruleset
  never emits — it emits `(7,53)`.
- **Unit ownership is explicit in the report.** `*` prefixes your own units, `-` foreign ones, and
  `+` a structure whose units are nested beneath it. The read-only rule for foreign units needs no
  inference.
- **The orders document ships inside the report**, as the `Orders Template (Long Format)` section.
  It already contains the `#atlantis` line, per-region banners, per-unit comment blocks and live
  orders. Import seeds the order draft from it. That line carries the faction password, so export
  must round-trip it and it must never be logged.
- **The map has four states**, not two: current, previously visited (data held but possibly stale),
  known by name only (from another region's `Exits` block), and unexplored. Staleness makes region
  records turn-versioned, so persistence needs a per-region last-seen turn.
- **Regions can be large.** One region in the turn 71 report holds 92 units across 24 structures,
  74 of them inside those structures, so the unit table needs virtualising and the side panels need
  summarise-then-expand. (The mockups say 63, a figure asserted before anyone counted; the parser
  in #19 counts 92 and asserts it in tests.)

## Caveats

- The set of "previously visited" hexes in `chosen-layout-turn71.html` is **illustrative**. Only one
  turn is available for that faction, so there is no genuine visit history to draw from. The state,
  its rendering and its behaviour are real; which specific hexes are stale is not.
- Colour, spacing and type here are indicative. The implementation uses Tailwind tokens as specified
  in `docs/implementation-plan.md`, and light/dark theming is issue #9's scope.
- The maps here are SVG, and so is the real renderer. It began on PixiJS and moved to SVG in #58,
  because a canvas bakes each label into a texture at one size: zoomed in, a settlement name was a
  nine-pixel bitmap magnified threefold. The stale hatch and the settlement glyph drawn here were
  part of that move.
