// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { FactionOrders } from "./FactionOrders";
import type { NewStudents } from "./NewStudents";
import type { OrderDiagnostic } from "./OrderDiagnostic";
import type { ProductionOverview } from "./ProductionOverview";
import type { UnitSilver } from "./UnitSilver";

/**
 * Validation result for one draft of Atlantis orders.
 */
export type OrderValidationResult = { diagnostics: Array<OrderDiagnostic>, 
/**
 * One entry per own unit the report shows, whether or not it has orders. `ah-1wcw.1`.
 *
 * `#[serde(default)]` so a cached or older payload deserialises to an empty list rather than
 * failing.
 */
silver: Array<UnitSilver>, 
/**
 * Every region this month's orders use a tax or trade slot in, for the Production window.
 * `ah-nneu`. `#[serde(default)]` so an older payload deserialises to no regions.
 */
production: ProductionOverview, 
/**
 * New quartermasters, mages and apprentices these orders make, for the faction view's
 * Allowances rows. `ah-x7s3`. `#[serde(default)]` so an older payload deserialises to zeros.
 */
students: NewStudents, 
/**
 * What this turn's FACTION orders do, for the faction dropdown. `ah-7g4f`.
 * `#[serde(default)]` so an older payload deserialises to "no FACTION order".
 */
faction: FactionOrders, };
