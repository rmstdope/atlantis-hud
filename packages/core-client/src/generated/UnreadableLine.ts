// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { LostBlock } from "./LostBlock";
import type { UnitRead } from "./UnitRead";
import type { UnreadableKind } from "./UnreadableKind";

/**
 * One record the parser could not read, kept verbatim so the player can see what was lost.
 */
export type UnreadableLine = { kind: UnreadableKind, 
/**
 * 1-based source line numbers, from `LogicalLine`. Equal when the record did not wrap.
 */
lineStart: number, lineEnd: number, 
/**
 * The joined logical line, exactly as the parser saw it.
 */
text: string, lost: LostBlock | null, 
/**
 * For a `Unit` row whose unit still reached the map, how much of it was read.
 *
 * `None` for every other row, and never `Some(UnitRead::Complete)` — a completely read unit is
 * not on this list at all.
 */
unitRead: UnitRead | null, };
