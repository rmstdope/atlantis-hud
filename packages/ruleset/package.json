{
  "name": "@atlantis/ruleset",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "//scrape": "Named 'scrape' rather than 'fetch' because `pnpm fetch` is a builtin command that would shadow it.",
    "scrape": "tsx src/cli.ts"
  }
}
