{
  "name": "atlantis-hud",
  "version": "0.9.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "lint": "pnpm -r run lint",
    "release": "tsx scripts/release.ts",
    "beads:export": "tsx scripts/beadsExport.ts",
    "generate:biomes": "tsx scripts/genBiomes.ts",
    "typecheck": "pnpm -r run typecheck && pnpm run typecheck:tooling && cargo check --workspace",
    "typecheck:tooling": "tsc --noEmit -p tsconfig.tooling.json",
    "test": "tsx scripts/runSuites.ts",
    "test:tooling": "vitest run --config vitest.tooling.config.ts",
    "test:smoke": "tsx scripts/withGateLock.ts pnpm exec playwright test",
    "test:native": "wdio run wdio.conf.ts",
    "build:web": "pnpm --filter @atlantis/web build",
    "test:pwa": "tsx scripts/withGateLock.ts pnpm exec playwright test --config playwright.pwa.config.ts",
    "check:generated": "tsx scripts/checkGenerated.ts",
    "check": "pnpm run lint && pnpm run typecheck && pnpm run test && pnpm run check:generated && pnpm run test:smoke && pnpm run build:web && pnpm run test:pwa && cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings",
    "check:fast": "pnpm run lint && pnpm run typecheck && pnpm run test && pnpm run check:generated && cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings"
  },
  "devDependencies": {
    "@atlantis/fixtures": "workspace:*",
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.10.2",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@typescript-eslint/eslint-plugin": "^8.40.0",
    "@typescript-eslint/parser": "^8.40.0",
    "@wdio/cli": "^9.20.0",
    "@wdio/globals": "^9.20.0",
    "@wdio/local-runner": "^9.20.0",
    "@wdio/mocha-framework": "^9.20.0",
    "@wdio/spec-reporter": "^9.20.0",
    "eslint": "^9.34.0",
    "eslint-plugin-react-hooks": "^5.2.0",
    "sharp": "^0.35.3",
    "tsx": "^4.23.11",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
